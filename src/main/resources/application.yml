spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1q2w3e4r
    url: jdbc:mysql://localhost:3306/songpyeon
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_branch_fetch_size: 1000
    hibernate:
      ddl-auto: create
    open-in-view: false
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false